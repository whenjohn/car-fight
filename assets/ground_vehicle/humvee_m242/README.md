# Humvee M242 vehicle asset

`HumveeM242.fbx` and `texture.png` were supplied together by the project owner
from the matching `HUMVEE_M242_FBX.rar` and `HUMVEE_M242_TEXTURE.rar` archives.
The texture must remain beside the FBX so Godot resolves the model's material.
